{
  "name": "resource-booking-system",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack company resource booking system (React + Node/Express + PostgreSQL)",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down",
    "backend:dev": "npm run dev --workspace backend",
    "backend:seed": "npm run seed --workspace backend",
    "frontend:dev": "npm run dev --workspace frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run backend:dev\" \"npm run frontend:dev\"",
    "lint": "npm run lint --workspace backend && npm run lint --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  } 
}
